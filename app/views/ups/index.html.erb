<p id="notice"><%= notice %></p>

<h1>Lista Ups</h1>

<table>
  <thead>
    <tr>
      <th>Usuario</th>
      <th>Adjunto</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @ups.each do |up| %>
      <tr>
        <td><%= up.usuario %></td>
        <td><%= up.adjunto %></td>
        <td><%= link_to 'Ver', up %></td>
        <td><%= link_to 'Editar', edit_up_path(up) %></td>
        <td><%= link_to 'Eliminar', up, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nueva Subida', new_up_path %>
